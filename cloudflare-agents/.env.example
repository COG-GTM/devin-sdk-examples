# A Devin API key (cog_…) from app.devin.ai → Settings → API keys.
DEVIN_API_KEY=
# Optional: the organization to create sessions in, when the key can reach several.
DEVIN_ORG_ID=
# Optional: a different Devin API origin.
DEVIN_BASE_URL=
